{% extends "LeyJusticiaBundle::layout.html.twig" %}

{% block content %}
    
    <div class="contenedor-login-crud"> 
        
        <h2 class="titulo-login-crud tags">Editar Perfil</h2>
        
        {{form_start(form, {'action': '', 'method': 'POST', 'attr' : { 'class' : 'login-crud' } })}}
            <label>Imagen: </label>
            <img src="{{ asset("users/"~user.image)}}" alt="{{user.name}}" class="articulo">
            {{form_widget(form.image)}}
        {{form_end(form)}}
        
    </div>
    
{% endblock %}
